{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Pedido",
  "type": "object",
  "required": ["situacao"],
  "properties": {
    "situacao": {
      "type": "string",
      "enum": ["ABERTO", "FECHADO"]
    }
  }
}
